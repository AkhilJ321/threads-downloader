import React from 'react';
import { ThreadsPost } from '../util/types/ThreadsPost'; // Replace this with the path to your TypeScript interfaces

const Thread = ({ data }: { data: any }) => {
  console.log(data);
  return <div>Hello</div>;
};

export default Thread;
